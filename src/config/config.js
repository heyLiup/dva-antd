// var arr='01010012001';
// // var reg=/(0*)$/g;
// var reg=/[0]*$/g;
// var length=arr.match(reg)[0].length; //后面0的位数
// var max=arr.length; //字符串总长度
// var les=max-length  //从头开始需要截取的长度
// var arr1=arr.substring(0,les);
// console.log(arr1);
module.exports = {
  apiPrefix: '/api/',
}
